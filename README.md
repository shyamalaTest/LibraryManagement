# MEAN + (Angular 5) Application + Passport Authentication

Steps >  To start application locally

* Run MongoDB server
* Run `npm install`
* Run `npm install --save-dev @angular-devkit/core`
* Run `npm start`
